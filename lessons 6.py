try:
      print("wassab")
except:

      print("pp")
else
      print("ff")

try:
      print("start code")
      print("No errors")
except NameError as error
      print(error)
else:
     print("I am ELSE")
finally:
     print("Finally code")
def checker(var_1):
    if type(var_1)!= str:
        raise TypeError(F"Sorry, wa can't work with {type(var_1)}, ")
    else
        return print(var_1)
first_var = "16"
checker (first_var)

import warnings
warnings.warn ("Warning, no code here", SyntaxWarning)

import warnings
warnings.warn ("Warning, no code here", SyntaxWarning)

import warnings
warnings .simplefilter ("ignore", SyntaxWarning)
warnings. simplefilter ("always", Importilarning).

warnings .warn("Warning, no code here", SyntaxWarning)
warnings .warn ("Warning, module not import", ImportWarning)

g = int (input("g = "))
1 = int (input (" = "))
try:
    print(g*L)
except TypeError as error:
    print (error)